#include<stdio.h>
int main(){
    int a,b,Sum;
    printf("Enter the values of a and b: ");
    scanf("%d",&a);
    scanf("%d",&b);
    Sum = (a+b);
    printf("The sum is: %d",Sum);
    return 0;
}